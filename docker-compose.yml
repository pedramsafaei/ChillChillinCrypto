version: '3.8'

services:
  crypto-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chillchillin-crypto
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - crypto-network

networks:
  crypto-network:
    driver: bridge
